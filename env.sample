# Browser Pool Configuration

# Maximum number of concurrent browsers (default: 2, minimum: 1)
BROWSER_MAX_BROWSERS=2

# Browser idle timeout in seconds before automatic shutdown (default: 300 = 5 minutes)
BROWSER_IDLE_TIMEOUT=300
